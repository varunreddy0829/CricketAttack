"""The scoreboard: run N innings through any ball function and compare the
resulting distributions against real IPL.

Without this, no change to the engine -- hand-tuned or learned -- is measurable.
`stats.summarize` is deliberately shared by the real-data path and the simulated
path so the two are always computed identically.
"""
